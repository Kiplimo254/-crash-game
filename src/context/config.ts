import { API_BASE_URL, WS_GAME_URL, WS_BETTING_URL } from '../config/env';

export const config = {
  api: API_BASE_URL,
  wsGame: WS_GAME_URL,
  wsBetting: WS_BETTING_URL,
  // Add any other configuration needed
};
